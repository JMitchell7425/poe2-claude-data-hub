name: Update PoE2 Data Bundle

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-data:
    name: Update data
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo "Workflow syntax is valid"
